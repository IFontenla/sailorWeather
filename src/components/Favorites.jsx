import FavoriteBoard from "./FavoriteBoard";

export default function Favorites(favorites, deleteFavorite) {
    return(
      <div>
        {/* {favorites && favorites.map((fav) => <FavoriteBoard />)} */}
      </div>

    );
  }